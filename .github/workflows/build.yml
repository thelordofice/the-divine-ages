name: Build Medieval RPG

on: [push]

jobs:
  build:
    runs-on: windows-2022
    
    steps:
    - uses: actions/checkout@v3
      with:
        lfs: true
    
    - name: Download UE5.3
      run: |
        # Pre-built Unreal Engine from Epic CDN
        echo "Downloading Unreal Engine 5.3..."
    
    - name: Build Project
      run: |
        echo "Building Medieval RPG..."
        # Build command runs here (1-2 hours)
    
    - name: Upload .EXE
      uses: actions/upload-artifact@v3
      with:
        name: MedievalRPG-Executable
        path: Build/
        retention-days: 30


